{
  "name": "lambda-project-sample",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "test": "run-s \"test:* {@}\" --",
    "test:app1": "yarn workspace @lambda-project-sample/function1 test",
    "build": "run-s \"build:* {@}\" --",
    "build:function1": "yarn workspace @lambda-project-sample/function1 build"
  },
  "devDependencies": {
    "@craco/craco": "^7.0.0",
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5"
  }
}
